[Unit]
Description=${DESCRIPTION} (self update)
After=docker.service
Requires=docker.service

[Service]
#Restart=always
TimeoutStartSec=0
ExecStart=${BIN_DIR}/update/run.sh ${MODULE_DIR}

[Install]
WantedBy=multi-user.target
